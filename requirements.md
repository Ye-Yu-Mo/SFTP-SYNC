# SFTP自动同步工具 - Requirements Document

基于rsync的跨平台文件自动同步工具。客户端指定远程服务器(IP/端口/用户名/密码)和同步目录，支持增量同步、自动同步间隔、版本备份(保留10个时间戳目录)、延迟删除(本地删除后远端保留60天)。只做单向push，不pull。Windows打包cwRsync，Linux/macOS使用系统rsync。使用gpui-component构建跨平台GUI。

## Core Features

1. **单向同步（Push Only）**
   - 本地文件自动推送到远程服务器
   - 不从远程拉取，避免冲突

2. **增量同步（基于 rsync）**
   - 只传输变化的文件
   - 使用 rsync 算法，高效传输

3. **自动同步**
   - 用户设置同步间隔（秒/分钟/小时）
   - 后台监控文件变化（notify crate）
   - 变化后自动触发同步

4. **版本备份（保留10个时间戳目录）**
   - 每次同步，变化的旧文件保存到 `.backup/YYYYmmdd-HHMMSS/` 目录
   - 最多保留10个最近的时间戳目录
   - 超过10个，自动删除最旧的

5. **延迟删除（本地删除后远端保留60天）**
   - 本地删除文件时，远端文件移动到 `.trash/` 目录
   - 本地维护删除记录（`~/.sftp-sync/deleted.json`）
   - 超过60天后，自动清理远端 `.trash/` 中的文件

6. **多任务管理**
   - 支持创建多个同步任务（每个任务对应一个远程服务器 + 目录对）
   - 任务汇总界面：显示所有任务的状态、最近同步时间
   - 任务详情界面：显示单个任务的详细信息、同步进度、历史日志
   - 点击任务卡片跳转到详情页面

7. **跨平台 GUI（gpui-component）**
   - 任务汇总界面：所有任务列表、状态概览
   - 任务详情界面：单个任务的配置、状态、进度、日志
   - 配置界面：服务器信息、同步目录、同步间隔
   - 支持 Windows/Linux/macOS

8. **rsync 工具集成**
   - Windows：打包 cwRsync 到 release
   - Linux/macOS：使用系统 rsync

## User Stories

- **作为开发者**，我想自动同步本地代码到远程服务器，这样我不用手动上传文件
- **作为用户**，我想保留文件历史版本，这样误操作可以恢复
- **作为用户**，我想删除的文件延迟真删，这样有反悔机会
- **作为用户**，我想设置自动同步间隔，这样不用手动触发同步
- **作为用户**，我想看到同步状态和进度，这样知道同步是否成功
- **作为用户**，我想管理多个同步任务，这样可以同时同步到多个服务器
- **作为用户**，我想在汇总界面看到所有任务状态，这样快速了解哪些任务正常、哪些失败

## Acceptance Criteria

### 配置管理
- [ ] 能配置远程服务器（IP、端口、用户名、密码）
- [ ] 能指定本地目录和远程目录
- [ ] 能设置自动同步间隔（秒/分钟/小时）
- [ ] 配置持久化（保存到 `~/.sftp-sync/config.json`）
- [ ] 密码加密存储（使用 keyring crate）

### 同步功能
- [ ] 能手动触发同步
- [ ] 能自动同步（按设置的间隔）
- [ ] 增量同步（只传输变化的文件）
- [ ] 同步失败时显示错误信息
- [ ] 同步成功时显示传输文件数量和大小

### 版本备份
- [ ] 变更的旧文件保存到 `.backup/YYYYmmdd-HHMMSS/` 目录
- [ ] 本地维护备份目录列表（`~/.sftp-sync/backups.json`）
- [ ] 最多保留10个备份时间戳目录
- [ ] 超过10个时，自动删除远端最旧的备份目录

### 延迟删除
- [ ] 本地删除文件时，远端文件移动到 `.trash/原路径/文件名.删除时间`
- [ ] 本地维护删除记录（`~/.sftp-sync/deleted.json`）
- [ ] 每次启动时检查删除记录，超过60天的自动清理
- [ ] 清理时使用 SSH 远程删除（`ssh rm -rf`）

### 多任务管理
- [ ] 支持创建多个同步任务
- [ ] 每个任务独立配置（服务器、目录、间隔）
- [ ] 任务列表持久化（`~/.sftp-sync/tasks.json`）
- [ ] 能启用/禁用单个任务
- [ ] 能删除任务

### GUI 界面

#### 任务汇总界面
- [ ] 显示所有任务卡片（任务名称、服务器、目录）
- [ ] 显示任务状态（同步中/空闲/失败/已禁用）
- [ ] 显示最近同步时间（如：2小时前）
- [ ] 显示同步结果（成功/失败，传输文件数量）
- [ ] 点击任务卡片跳转到详情页面
- [ ] 新建任务按钮

#### 任务详情界面
- [ ] 配置界面：服务器信息输入
- [ ] 配置界面：目录选择
- [ ] 配置界面：同步间隔设置
- [ ] 状态显示：连接状态（已连接/未连接）
- [ ] 状态显示：同步状态（同步中/空闲/失败）
- [ ] 状态显示：同步进度（百分比/文件数量）
- [ ] 历史日志：同步记录、错误日志
- [ ] 返回按钮（回到汇总界面）

### 跨平台
- [ ] Windows 平台运行正常
- [ ] Linux 平台运行正常
- [ ] macOS 平台运行正常
- [ ] GitHub Action 自动构建 release

## Non-functional Requirements

### 性能
- 支持大文件（GB 级别）
- 支持大量文件（10000+ 文件）
- 同步时不阻塞 GUI 主线程
- 文件监控（notify）不占用过多 CPU

### 安全
- 密码加密存储（keyring crate）
- SSH 连接加密传输
- 不在日志中明文记录密码

### 兼容性
- Windows 10/11
- Linux（Ubuntu 20.04+）
- macOS 12+
- Rust edition 2021

### 可靠性
- 同步失败自动重试（最多3次）
- 网络中断后自动重连
- 程序崩溃后配置不丢失
